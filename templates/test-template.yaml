spec:
  templates:

  - name: check-types
    parent: generic-task
    inputs:
      parameters:
      - name: p1
      - name: p2
      - name: command
        value: python3 main_script.py
      artifacts:
      - name: main-script
        path: /app/main_script.py
        raw:
          data: |
            print(type({{inputs.parameters.p1}}))
            print(type({{inputs.parameters.p2}}))
